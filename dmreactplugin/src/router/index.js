import React from 'react';
import { Route, Switch } from 'react-router-dom';

const Routes = () => {
    return (
        <Switch>
            <Route exact path='' />
            <Route exact path='' />
            <Route exact path='' />
            <Route exact path='' />
            <Route exact path='' />
            <Route exact path='' />
        </Switch>
    );
};

export default Routes;
